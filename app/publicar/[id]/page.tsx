import { notFound } from "next/navigation"; import DressPublishForm from "@/components/DressPublishForm"; import { requireUser } from "@/lib/auth";
export default async function EditDress({params}:{params:{id:string}}){ const {supabase,user}=await requireUser(); const [{data:dress},{data:brands}]=await Promise.all([supabase.from("dresses").select("*,dress_photos(*)").eq("id",params.id).eq("seller_id",user.id).maybeSingle(),supabase.from("brands").select("id,name").eq("is_active",true).order("name")]); if(!dress)notFound(); return <main className="page"><DressPublishForm initialDress={dress} brands={brands??[]} userId={user.id}/></main> }
