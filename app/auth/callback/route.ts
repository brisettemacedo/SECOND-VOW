import {createClient} from "@/lib/supabase/server";import {NextResponse} from "next/server";import {safeInternalPath} from "@/lib/navigation";
export async function GET(request:Request){const url=new URL(request.url);const code=url.searchParams.get("code");const next=safeInternalPath(url.searchParams.get("next"));const pendingDressId=url.searchParams.get("dress");const origin=url.origin;if(code){const supabase=await createClient();const {data,error}=await supabase.auth.exchangeCodeForSession(code);if(!error){if(data.user){const name=String(data.user.user_metadata?.full_name||data.user.user_metadata?.name||"").trim();if(name)await supabase.from("profiles").update({full_name:name}).eq("id",data.user.id);if(pendingDressId)await supabase.from("favorites").upsert({user_id:data.user.id,dress_id:pendingDressId},{onConflict:"user_id,dress_id"})}return NextResponse.redirect(`${origin}${next}`)}}return NextResponse.redirect(`${origin}/login?error=auth_callback_failed`)}
