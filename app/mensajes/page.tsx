import MessagesClient from "@/components/MessagesClient"; import { requireUser } from "@/lib/auth";
export default async function MessagesPage(){const {supabase,user}=await requireUser();const {data}=await supabase.from("conversations").select("id,dress_id,buyer_id,seller_id,last_message_at,dresses(model,brands(name)),messages(id,sender_id,body,created_at,read_at)").or(`buyer_id.eq.${user.id},seller_id.eq.${user.id}`).order("last_message_at",{ascending:false}); const initial=(data??[]).map((c:any)=>({...c,messages:[...(c.messages??[])].sort((a:any,b:any)=>a.created_at.localeCompare(b.created_at))}));return <main className="page"><h1>Mensajes</h1><MessagesClient initial={initial as any} userId={user.id}/></main>}
