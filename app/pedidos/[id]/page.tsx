import {notFound} from "next/navigation";import {requireUser} from "@/lib/auth";import OrderActions from "@/components/OrderActions";
export default async function OrderDetail({params}:{params:{id:string}}){const {supabase,user}=await requireUser();const {data:o}=await supabase.from("orders").select("*,dresses(model),claims(*),shipments(*)").eq("id",params.id).or(`buyer_id.eq.${user.id},seller_id.eq.${user.id}`).maybeSingle();if(!o)notFound();return <main className="page narrow"><h1>Pedido</h1><section className="panel"><h2>{o.dresses?.model||"Vestido"}</h2><p>Total: ${Number(o.total_mxn).toLocaleString("es-MX")} MXN</p><p>Estado: <span className="badge">{o.status}</span></p>{o.tracking_number&&<p>Envío: {o.carrier} | {o.tracking_number}</p>}</section><OrderActions order={o} userId={user.id}/>{o.claims?.length>0&&<section className="panel"><h2>Reclamaciones</h2>{o.claims.map((c:any)=><div key={c.id}><strong>{c.status}</strong><p>{c.description}</p></div>)}</section>}</main>}
