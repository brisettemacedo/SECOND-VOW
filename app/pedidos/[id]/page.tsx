import Link from "next/link";import {notFound} from "next/navigation";import {requireUser} from "@/lib/auth";import OrderActions from "@/components/OrderActions";
function cleanModel(v:any){const s=String(v??"").trim();return /^(na|n\/?a|no aplica|sin modelo)$/i.test(s)?"":s}
export default async function OrderDetail({params}:{params:{id:string}}){const {supabase,user}=await requireUser();const {data:o}=await supabase.from("orders").select("*,dresses(id,model,brands(name)),claims(*),shipments(*)").eq("id",params.id).or(`buyer_id.eq.${user.id},seller_id.eq.${user.id}`).maybeSingle();if(!o)notFound();const{data:evidence}=await supabase.from("order_evidence").select("id,evidence_type,storage_path,created_at,uploaded_by").eq("order_id",o.id).order("created_at");const model=cleanModel(o.dresses?.model);const title=[o.dresses?.brands?.name,model].filter(Boolean).join(" ")||"Vestido";return <main className="page narrow"><div className="title-row"><div><span className="muted">Pedido</span><h1><Link href={`/vestidos/${o.dress_id}`} target="_blank" rel="noopener noreferrer">{title}</Link></h1></div><Link className="btn btn-secondary" href={`/vestidos/${o.dress_id}`} target="_blank" rel="noopener noreferrer">Ver vestido</Link></div><section className="panel"><p>Vestido: ${Number(o.subtotal_mxn).toLocaleString("es-MX")} MXN</p>{o.shipping_quote_set_at?<p>Envío: ${Number(o.shipping_mxn).toLocaleString("es-MX")} MXN{o.shipping_carrier_declared?` con ${o.shipping_carrier_declared}`:""}</p>:<p className="muted">Envío pendiente de cotización.</p>}<p>Estado: <span className="badge">{o.status}</span></p>{o.tracking_number&&<p>Rastreo: {o.carrier} | {o.tracking_number}</p>}</section><OrderActions order={o} userId={user.id} evidence={evidence??[]}/>{o.claims?.length>0&&<section className="panel"><h2>Reclamaciones</h2>{o.claims.map((c:any)=><div key={c.id}><strong>{c.status}</strong><p>{c.description}</p></div>)}</section>}</main>}
