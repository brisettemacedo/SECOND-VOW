import IdentityVerificationForm from "@/components/IdentityVerificationForm";import {requireUser} from "@/lib/auth";
export default async function Verify(){const {supabase,user}=await requireUser();const {data}=await supabase.from("identity_verifications").select("status,created_at,document_type,document_deleted_at,verification_result").eq("user_id",user.id).order("created_at",{ascending:false}).limit(1).maybeSingle();return <main className="page narrow"><IdentityVerificationForm userId={user.id} current={data}/></main>}
