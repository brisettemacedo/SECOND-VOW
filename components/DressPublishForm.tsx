"use client";
import {useEffect,useMemo,useState} from "react";import {useRouter} from "next/navigation";import {createClient} from "@/lib/supabase/client";import type {DressCatalogData} from "@/lib/dressCatalogData";import {TERMS_VERSION} from "@/lib/site";
type Brand={id:string;name:string};type Dress=Record<string,any>&{id?:string;status?:string;dress_photos?:any[];brand_suggestions?:any};
const stepNames=["Identidad","Talla y medidas","Diseño","Condición","Alteraciones","Precio","Envío","Descripción","Fotografías","Revisión"];
const fieldsByStep:string[][]=[["brand_id","brand_suggestion_id","model","collection","year_approx"],["talla_etiqueta","sistema_talla","busto_cm","cintura_cm","cadera_cm","largo_hombro_piso_cm","altura_persona_cm","altura_tacon_cm","puede_ampliarse","puede_reducirse"],["silueta","escote","espalda","manga","tela_principal","tela_secundaria","color_principal","color_forro","cola","cola_largo_cm"],["condicion","tiene_manchas","tiene_jalones","tiene_roturas","dano_dobladillo","falta_aplicaciones","tiene_reparaciones","tiene_decoloracion","descripcion_danos"],["tuvo_ajustes","ajustes_detalle","conserva_margen_costura"],["precio_original_mxn","precio_venta_mxn"],["envio_nacional"],["descripcion"],[],[]];
const numeric=new Set(["year_approx","busto_cm","cintura_cm","cadera_cm","largo_hombro_piso_cm","altura_persona_cm","altura_tacon_cm","cola_largo_cm","precio_original_mxn","precio_venta_mxn"]);
export default function DressPublishForm({initialDress,brands,catalogs,userId}:{initialDress?:Dress;brands:Brand[];catalogs:DressCatalogData;userId:string}){const supabase=useMemo(()=>createClient(),[]);const router=useRouter();const initialBrand=initialDress?.brand_id?brands.find(b=>b.id===initialDress.brand_id)?.name||"":initialDress?.brand_suggestions?.suggested_name||"";const [step,setStep]=useState(0);const [dress,setDress]=useState<Dress>(initialDress??{seller_id:userId,status:"draft",sistema_talla:"MX",envio_nacional:true});const [brandQuery,setBrandQuery]=useState(initialBrand);const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);const [photos,setPhotos]=useState<any[]>(initialDress?.dress_photos??[]);const [selectedCharacteristics,setSelectedCharacteristics]=useState<string[]>(initialDress?.dress_characteristics?.map((x:any)=>x.characteristic_id)??[]);const [decl,setDecl]=useState({authentic:false,photos:false,right:false,trueInfo:false});
const exactBrand=brands.find(b=>b.name.localeCompare(brandQuery.trim(),undefined,{sensitivity:'base'})===0);const matches=brandQuery.trim()?brands.filter(b=>b.name.toLowerCase().includes(brandQuery.trim().toLowerCase())).slice(0,8):brands.slice(0,8);
async function ensureDraft(){if(dress.id)return dress.id;const {data,error}=await supabase.from("dresses").insert({seller_id:userId,status:"draft",sistema_talla:"MX",envio_nacional:true}).select("id").single();if(error)throw error;setDress(d=>({...d,id:data.id}));return data.id}
async function chooseBrand(b:Brand){setBrandQuery(b.name);setDress(d=>({...d,brand_id:b.id,brand_suggestion_id:null}))}
async function suggestBrand(){const name=brandQuery.trim();if(name.length<2)return setMessage('Escribe el nombre de la marca.');setBusy(true);try{const id=await ensureDraft();const {data,error}=await supabase.from('brand_suggestions').insert({suggested_name:name,seller_id:userId,dress_id:id}).select('id,suggested_name,status').single();if(error)throw error;setDress(d=>({...d,brand_id:null,brand_suggestion_id:data.id,brand_suggestions:data}));setMessage('Marca enviada para revisión. Puedes seguir guardando tu borrador.')}catch(e:any){setMessage(e.message)}finally{setBusy(false)}}
async function save(){setBusy(true);setMessage("");try{const id=await ensureDraft();const payload:any={};for(const f of fieldsByStep.flat())if(f in dress){const v=dress[f];payload[f]=numeric.has(f)?(v===""||v==null?null:Number(v)):v}const {error}=await supabase.from("dresses").update(payload).eq("id",id);if(error)throw error;const {error:deleteChars}=await supabase.from("dress_characteristics").delete().eq("dress_id",id);if(deleteChars)throw deleteChars;if(selectedCharacteristics.length){const {error:insertChars}=await supabase.from("dress_characteristics").insert(selectedCharacteristics.map(characteristic_id=>({dress_id:id,characteristic_id})));if(insertChars)throw insertChars}setMessage("Borrador guardado.");return id}catch(e:any){setMessage(e.message);throw e}finally{setBusy(false)}}
async function upload(files:FileList|null){if(!files?.length)return;setBusy(true);try{const id=await ensureDraft();for(const [i,file] of Array.from(files).entries()){const ext=file.name.split('.').pop()?.toLowerCase()||'jpg';const path=`${userId}/${id}/${crypto.randomUUID()}.${ext}`;const {error:up}=await supabase.storage.from("dress-images").upload(path,file,{upsert:false});if(up)throw up;const {data,error}=await supabase.from("dress_photos").insert({dress_id:id,storage_path:path,position:photos.length+i,is_primary:photos.length+i===0,classification:"frontal"}).select().single();if(error)throw error;setPhotos(p=>[...p,data])}setMessage("Fotografías subidas.")}catch(e:any){setMessage(e.message)}finally{setBusy(false)}}
async function submit(){setBusy(true);try{const id=await save();if(photos.length<3)throw new Error("Sube al menos 3 fotografías antes de enviar a revisión.");if(!Object.values(decl).every(Boolean))throw new Error("Debes aceptar las cuatro declaraciones de publicación.");const {error:de}=await supabase.from('dress_declarations').upsert({dress_id:id,seller_id:userId,authenticity_declared:decl.authentic,photos_correspond_declared:decl.photos,right_to_sell_declared:decl.right,information_true_declared:decl.trueInfo,terms_version:TERMS_VERSION,declared_at:new Date().toISOString()},{onConflict:'dress_id'});if(de)throw de;const {error}=await supabase.from("dresses").update({status:"pending_review"}).eq("id",id);if(error)throw error;router.push("/mis-vestidos");router.refresh()}catch(e:any){setMessage(e.message)}finally{setBusy(false)}}
function set(name:string,value:any){setDress(d=>({...d,[name]:value}))}function options(name:string,label:string,opts:{value:string;label:string}[]){return <div className="field"><label>{label}</label><select value={dress[name]??""} onChange={e=>set(name,e.target.value||null)}><option value="">Selecciona</option>{opts.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select></div>}function input(name:string,label:string,type="text"){return <div className="field"><label>{label}</label><input type={type} value={dress[name]??""} onChange={e=>set(name,e.target.value)}/></div>}function check(name:string,label:string){return <label className="check"><input type="checkbox" checked={Boolean(dress[name])} onChange={e=>set(name,e.target.checked)}/><span>{label}</span></label>}
return <div className="wizard"><div className="stepper">{stepNames.map((n,i)=><button key={n} type="button" className={i===step?"active":""} onClick={()=>setStep(i)}>{i+1}. {n}</button>)}</div><section className="panel"><h1>{initialDress?.id?"Editar vestido":"Publicar vestido"}</h1><h2>{stepNames[step]}</h2>
{step===0&&<><div className="field brand-search"><label>Marca</label><input value={brandQuery} onChange={e=>{setBrandQuery(e.target.value);set('brand_id',null)}} placeholder="Empieza a escribir una marca…"/><div className="brand-suggestions">{matches.map(b=><button type="button" key={b.id} onClick={()=>chooseBrand(b)}>{b.name}</button>)}</div>{brandQuery.trim()&&!exactBrand&&!dress.brand_suggestion_id&&<div className="brand-new"><p>No encontramos una coincidencia exacta.</p><strong>Nombre de la marca</strong><div>{brandQuery}</div><button type="button" className="btn btn-secondary" disabled={busy} onClick={suggestBrand}>Enviar marca para revisión</button></div>}{dress.brand_suggestion_id&&<p className="muted">Marca sugerida: <strong>{dress.brand_suggestions?.suggested_name||brandQuery}</strong> · pendiente de revisión.</p>}</div>{input("model","Modelo")}{input("collection","Colección")}{input("year_approx","Año aproximado","number")}</>}
{step===1&&<><div className="grid-2">{options("talla_etiqueta","Talla de etiqueta",catalogs.sizes)}{options("sistema_talla","Sistema de talla",catalogs.sizingSystems)}{input("busto_cm","Busto (cm)","number")}{input("cintura_cm","Cintura (cm)","number")}{input("cadera_cm","Cadera (cm)","number")}{input("largo_hombro_piso_cm","Hombro a piso (cm)","number")}{input("altura_persona_cm","Altura de quien lo usó (cm)","number")}{input("altura_tacon_cm","Altura de tacón (cm)","number")}</div>{check("puede_ampliarse","Puede ampliarse")}{check("puede_reducirse","Puede reducirirse")}</>}
{step===2&&<><div className="grid-2">{options("silueta","Silueta",catalogs.silhouettes)}{options("escote","Escote",catalogs.necklines)}{options("espalda","Espalda",catalogs.backs)}{options("manga","Manga",catalogs.sleeves)}{options("tela_principal","Tela principal",catalogs.fabrics)}{options("tela_secundaria","Tela secundaria",catalogs.fabrics)}{options("color_principal","Color principal",catalogs.colors)}{input("color_forro","Color del forro")}{options("cola","Cola",catalogs.trains)}{input("cola_largo_cm","Largo de cola (cm)","number")}</div><fieldset className="filter-group"><legend>Características y detalles</legend><div className="filter-options">{catalogs.characteristics.map(c=><label key={c.id} className="filter-check"><input type="checkbox" checked={selectedCharacteristics.includes(c.id)} onChange={()=>setSelectedCharacteristics(current=>current.includes(c.id)?current.filter(x=>x!==c.id):[...current,c.id])}/><span>{c.label}</span></label>)}</div></fieldset></>}
{step===3&&<>{options("condicion","Condición",catalogs.conditions)}<div className="checks">{check("tiene_manchas","Tiene manchas")}{check("tiene_jalones","Tiene jalones")}{check("tiene_roturas","Tiene roturas")}{check("dano_dobladillo","Daño en dobladillo")}{check("falta_aplicaciones","Faltan aplicaciones")}{check("tiene_reparaciones","Tiene reparaciones")}{check("tiene_decoloracion","Tiene decoloración")}</div><div className="field"><label>Describe daños o imperfecciones</label><textarea value={dress.descripcion_danos??""} onChange={e=>set("descripcion_danos",e.target.value)} rows={5}/></div></>}
{step===4&&<>{check("tuvo_ajustes","Tuvo ajustes o alteraciones")}<div className="field"><label>Detalle de ajustes</label><textarea rows={5} value={dress.ajustes_detalle??""} onChange={e=>set("ajustes_detalle",e.target.value)}/></div>{check("conserva_margen_costura","Conserva margen de costura")}</>}
{step===5&&<div className="grid-2">{input("precio_original_mxn","Precio original (MXN)","number")}{input("precio_venta_mxn","Precio de venta (MXN)","number")}</div>}{step===6&&<><p>Todos los vestidos publicados en SECOND VOW deben estar disponibles para envío.</p><p className="muted">No se ofrecen pruebas ni entregas presenciales.</p></>}{step===7&&<div className="field"><label>Descripción adicional</label><textarea rows={10} value={dress.descripcion??""} onChange={e=>set("descripcion",e.target.value)} placeholder="Cuenta libremente la historia, detalles, accesorios incluidos o cualquier dato adicional relevante."/></div>}{step===8&&<><div className="field"><label>Fotografías (mínimo 3)</label><input type="file" accept="image/*" multiple onChange={e=>upload(e.target.files)}/></div><div className="photo-list">{photos.map((p,i)=><div key={p.id}>Foto {i+1}{p.is_primary?" · principal":""}</div>)}</div></>}
{step===9&&<div className="publish-declarations"><p>Antes de enviar a revisión confirma lo siguiente:</p><label className="check"><input type="checkbox" checked={decl.authentic} onChange={e=>setDecl(d=>({...d,authentic:e.target.checked}))}/><span>Declaro bajo protesta que el vestido es auténtico y no una falsificación.</span></label><label className="check"><input type="checkbox" checked={decl.photos} onChange={e=>setDecl(d=>({...d,photos:e.target.checked}))}/><span>Declaro que las fotografías corresponden al vestido anunciado.</span></label><label className="check"><input type="checkbox" checked={decl.right} onChange={e=>setDecl(d=>({...d,right:e.target.checked}))}/><span>Declaro que soy propietaria o tengo derecho para venderlo.</span></label><label className="check"><input type="checkbox" checked={decl.trueInfo} onChange={e=>setDecl(d=>({...d,trueInfo:e.target.checked}))}/><span>Declaro que la información publicada es verdadera y completa respecto de los aspectos relevantes.</span></label><p className="muted">Estas declaraciones se registran con fecha y versión de los Términos y Condiciones.</p></div>}
{message&&<div className={message.includes("guardado")||message.includes("subidas")||message.includes("enviada")?"alert-success":"alert-error"}>{message}</div>}<div className="wizard-actions"><button className="btn btn-secondary" disabled={step===0||busy} onClick={()=>setStep(s=>s-1)}>Anterior</button><button className="btn btn-secondary" disabled={busy} onClick={save}>Guardar</button>{step<9?<button className="btn btn-primary" disabled={busy} onClick={async()=>{try{await save();setStep(s=>s+1)}catch{}}}>Siguiente</button>:<button className="btn btn-primary" disabled={busy} onClick={submit}>Enviar a revisión</button>}</div></section></div>}
