"use client"; import {useMemo,useState} from "react"; import {createClient} from "@/lib/supabase/client"; import {useRouter} from "next/navigation";
export default function OfferButton({dressId,price,userId,sellerId}:{dressId:string;price:number;userId?:string;sellerId:string}){const supabase=useMemo(()=>createClient(),[]);const router=useRouter();const [open,setOpen]=useState(false);const [amount,setAmount]=useState(Math.round(price*.9));const [msg,setMsg]=useState("");async function send(){if(!userId){router.push(`/login?next=/vestidos/${dressId}`);return}const {error}=await supabase.from("offers").insert({dress_id:dressId,buyer_id:userId,seller_id:sellerId,amount_mxn:amount,status:"pending"});if(error)setMsg(error.message);else{setMsg("Oferta enviada.");setOpen(false)}}if(userId===sellerId)return null;return <div>{!open?<button className="btn btn-secondary" onClick={()=>setOpen(true)}>Hacer oferta</button>:<div className="inline-offer"><input type="number" min={1} max={price} value={amount} onChange={e=>setAmount(Number(e.target.value))}/><button className="btn btn-primary" onClick={send}>Enviar oferta</button><button className="btn btn-secondary" onClick={()=>setOpen(false)}>Cancelar</button></div>}{msg&&<small>{msg}</small>}</div>}
