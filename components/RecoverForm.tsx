"use client";
import {useState} from "react";import {createClient} from "@/lib/supabase/client";
export default function RecoverForm(){const supabase=createClient();const[email,setEmail]=useState("");const[sent,setSent]=useState(false);const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/actualizar-password`});setLoading(false);setSent(true)}return <main style={{maxWidth:400,margin:"0 auto",padding:"64px 24px"}}><h1 style={{fontSize:26,marginBottom:24}}>Recupera tu contraseña</h1>{sent?<div className="alert-success">Si existe una cuenta, recibirás instrucciones por correo.</div>:<form onSubmit={submit}><div className="field"><label htmlFor="email">Correo electrónico</label><input id="email" type="email" required autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></div><button className="btn btn-primary" disabled={loading} style={{width:"100%"}}>{loading?"Enviando...":"Enviar instrucciones"}</button></form>}</main>}
