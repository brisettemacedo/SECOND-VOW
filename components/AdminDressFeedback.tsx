"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";
export default function AdminDressFeedback({dressId}:{dressId:string}){const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);const [sent,setSent]=useState(false);async function send(){setBusy(true);const {error}=await createClient().rpc("admin_suggest_dress_changes",{p_dress_id:dressId,p_message:message.trim()});setBusy(false);if(error)alert(error.message);else{setSent(true);setMessage("")}}return <section className="panel"><h2>Sugerir una mejora</h2><p className="muted">Notifica a la vendedora si la información o las fotos no coinciden. Esto no despublica el vestido.</p><textarea rows={4} value={message} onChange={e=>setMessage(e.target.value)} placeholder="Ej. La foto principal no permite ver el vestido completo…"/>{sent&&<div className="alert-success">Notificación enviada a la vendedora.</div>}<button className="btn btn-primary" disabled={busy||message.trim().length<10} onClick={send}>Enviar sugerencia</button></section>}
